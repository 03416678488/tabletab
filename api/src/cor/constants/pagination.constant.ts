export const PAGINATION_DEFAULT_PER_PAGE = 10;
export const PAGINATION_DEFAULT_PAGE = 1;
/** Hard cap on page size — unbounded perPage lets one request scan a whole table. */
export const PAGINATION_MAX_PER_PAGE = 500;
