import { EventInquiryFlow } from "@/features/event/components/event-inquiry-flow";

/** Customer-facing event booking page (public — no account needed). */
export default function EventsPage() {
  return <EventInquiryFlow />;
}
